{
  "name": "paystackpayments",
  "version": "1.0.0",
  "description": "A node project to learn payment integration with paystack api for backend",
  "main": "server.js",
  "scripts": {
    "start":"node server.js",
    "build": "cd client && npm run build",
    "client-install": "cd client && npm install",
    "heroku-postbuild": "npm run client-insall && npm run build",
    "dev": "nodemon server.js --ignore client"
  },
  "author": "Harrison Osagiede",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.24.0",
    "bcrypt": "^5.0.1",
    "connect-mongo": "^4.3.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "joi": "^17.4.0",
    "mongoose": "^5.12.1",
    "passport": "^0.4.1",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.7"
  }
}
